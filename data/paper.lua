
-- wallperpaper

--@set
local StopCmd  = "关闭壁纸"
local OnStop   = "关了"
local StartCmd = "开启壁纸"
local OnStart  = "kaile" 


local module = {
    _StartCmd   = StartCmd,
    _StopCmd    = StopCmd,
    _OnStart    = onStart,
    _OnStop     = OnStop,
}

return module